# ==================== BASE DE DATOS ====================
# URL de conexión a PostgreSQL (Vercel Postgres o local)
DATABASE_URL="postgresql://user:password@localhost:5432/docnt?schema=public"

# Direct connection URL para Prisma Migrate (necesario para Vercel Postgres)
DIRECT_URL="postgresql://user:password@localhost:5432/docnt?schema=public"

# ==================== NEXTAUTH ====================
# Secreto para NextAuth (generar con: openssl rand -base64 32)
NEXTAUTH_SECRET="tu-secreto-aqui-generar-con-openssl"

# URL de tu aplicación (en producción usar la URL real)
NEXTAUTH_URL="http://localhost:3000"

# ==================== OAUTH PROVIDERS (Opcional) ====================
# GitHub OAuth
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Google OAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ==================== VERCEL BLOB STORAGE ====================
# URL de conexión a Vercel Blob (opcional, para producción)
BLOB_READ_WRITE_TOKEN=""
